
export type TourCard = {
    slug: string,
    _id: string,
    title: string,
    subtitle: string,
    minDuration: string,
    maxDuration: string,
    packageCount: number,
    minPrice: number,
    maxPrice: number,
}